# Pin a published release image; change deliberately when upgrading.
PRETENDER_IMAGE=ghcr.io/redstonexs/pretender:v1.0.2

# API key for the selected LLM provider.
PRETENDER_LLM_API_KEY=
# Generate with: openssl rand -hex 32; use the same token in NapCat.
ONEBOT_ACCESS_TOKEN=
